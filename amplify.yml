version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo $PWD
        - cd src
        - ls
        - cd ..
        - yarn install
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: build
    files:
      - '*/*'
  cache:
    paths:
      - node_modules/*/*
